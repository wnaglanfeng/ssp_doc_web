export interface UpdateLog {
  version: string;
  date: string;
  content: string;
}

export interface ProductInfo {
  product: string;
  version: string;
  date: string;
  content: string;
}

export interface FeatureCard {
  icon: string;
  title: string;
  description: string;
  color: string;
}